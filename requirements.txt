# Web Framework & API Server
fastapi>=0.110.0
uvicorn>=0.28.0
python-multipart>=0.0.9
requests>=2.31.0

# LLM Framework & RAG Integration
langchain>=0.1.13
langchain-community>=0.0.29
# langchain-text-splitters는 명시하지 않아도 langchain-community -> langchain-classic
# 경유로 이미 설치된다(확인됨). langchain-chroma는 쓰지 않는다(agent/chroma_store.py가
# chromadb를 직접 씀 — langchain_chroma를 import하면 langchain_core.tracers ->
# langsmith -> xxhash까지 전부 로드되는데, LangSmith 트레이싱은 이 프로젝트에서 쓰지
# 않는 기능이고 사내 Windows PC의 애플리케이션 제어 정책이 xxhash의 네이티브 DLL을
# 차단하는 사례가 있었다). langchain-chroma를 유일하게 쓰던 generator.py/preprocess_
# specs.py(레거시 PPTX 변환 파이프라인)를 삭제하면서 이 패키지도 완전히 제거했다.

# Vector DB
chromadb>=0.4.24

# Data Validation & Parsing
pydantic>=2.6.4
# python-pptx: PPTX 생성/변환 기능(제거됨)이 아니라, sample_specs/에 .pptx 원본
# 문서가 섞여 있을 경우 RAG가 이를 읽어 색인하기 위해 build_rag_ollama.py/agent/
# spec_retriever.py가 여전히 사용한다.
python-pptx>=0.6.23
python-docx>=1.1.0

# Environment Variables
python-dotenv>=1.0.1